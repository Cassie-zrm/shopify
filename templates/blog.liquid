<h1>{{ blog.title }}</h1>
{% for article in blog.articles %}
  <div>
    <a href="{{ article.url }}">
      <div>{{ article.title }}</div>
      <div>{{ article.author }}</div>
      <div>{{ article.excerpt }}</div>
    </a>
  </div>
{% endfor %}
